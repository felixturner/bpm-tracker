{
  "name": "bpm-tracker",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.js --bundle --outfile=dist/app.js --serve=8000 --servedir=dist --sourcemap",
    "build": "esbuild src/main.js --bundle --minify --outfile=dist/app.js "
  },
  "devDependencies": {
    "esbuild": "^0.17.4"
  },
  "dependencies": {
    "gsap": "^3.11.4",
    "lil-gui": "^0.19.2"
  }
}
